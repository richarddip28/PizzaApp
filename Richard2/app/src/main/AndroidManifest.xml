<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="richard.dip">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Richard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Specials_Screen" android:parentActivityName=".Richard"/>
        <activity android:name=".Order_Screen" android:parentActivityName=".Richard"/>
        <activity android:name=".Details_Screen" android:parentActivityName=".Richard"/>
        <activity android:name=".Confirmation_Screen" android:parentActivityName=".Richard"></activity>
    </application>

</manifest>